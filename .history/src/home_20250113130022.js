function toggleMenu() {
    const menu = document.querySelector('.navbar .center-links');
    const connect = document.querySelector('.navbar .connect');
    menu.classList.toggle('active');
    connect.classList.toggle('active');
}